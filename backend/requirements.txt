Flask==2.3.3
gunicorn==21.2.0
pymongo==4.4.0        # Only if you’re using MongoDB
requests==2.31.0      # Only if you make HTTP requests
python-dotenv==1.0.0  # Only if you use .env files for secrets
